# Update — 2026-06-13

Result of executing the review task list against the Skills Framework
(public GitHub HEAD `0d77213`). Tasks are grouped: **framework-level** first,
then **per-card**, each card in its own block. Cards with no findings are listed
in the clean roster at the end.

> **Source caveat.** Run against the public repo, not your local working copy.
> Re-run the validator commands locally to confirm before acting if you have
> unpushed changes.

## What was executed

- **Mechanical + reference pass over all 263 cards** (frontmatter integrity, id
  uniqueness, manifest↔disk parity, every `connections[].id` and every `refs[]`
  skill id resolved, body-section presence, placeholders, encoding). Reliable
  and complete.
- **Full audit of all 21 chains** (reference validity + step-sequence coherence).
- **Deep editorial read** of the newest and highest-risk cards: #263, all of
  Applied-AI, all of Consulting-Craft, plus a cross-category spot-check
  (Psychology, Systems, Economics, Business, Delivery & Flow, Leadership,
  Mathematics, Network Effects).

**Honest scope note:** the mechanical/reference layer covers 100% of cards; the
*editorial* (factual/clarity/omission) layer was a deep sample, not a line-by-line
read of all 263 bodies. The companion `review-task-list.md` remains the tracker
for completing that editorial pass card by card.

## Verdict

The framework is in **excellent shape**. Reference integrity, frontmatter, and
section structure are clean across the corpus; the chains are coherent; the cards
read are accurate and concrete. There are **no broken references, no duplicate
IDs, no missing required fields, and no missing sections.** The real update tasks
are a small set of framework-level items plus a handful of per-card refinements.

Several "findings" from the automated sweep were verified as **false positives**
and are *not* tasks: the 264 "broken links" are one intentional footer link to
`../../Skills%20Framework.md` (the file exists; the checker didn't URL-decode the
space); the "doubled words" are real book titles (e.g. *The New New Product
Development Game*); the "missing year" references are correct ancient/period
citations (Plato c. 380 BCE, Sun Tzu c. 500 BCE, Aristotle c. 350 BCE).

Validator baseline: `validate-cards` ✅ (0 errors), `check-counts` ✅,
`reconcile-refs` ✅, unit tests ✅, `snapshot-test` ❌ (stale baseline — see FW-1).

---

# Framework-level update tasks

## FW-1 — Re-baseline the snapshot test  *(P0 — only failing check)*
`snapshot-test.mjs` reports 9 drifts. This is a **stale fixture**, not a data
defect: the baseline at `scripts/fixtures/baseline/manifest-snapshot.json` was
captured 2026-06-09 (262 skills) and predates card #263 and a connection-graph
enrichment. `reconcile-refs` and `check-counts` both pass against the current
manifest. Spot-check the large `connectionLines` jump (1723→2153) to confirm it's
deliberate authoring, then refresh:
```bash
node scripts/snapshot-test.mjs --write
git add scripts/fixtures/baseline/ && git commit -m "chore: re-baseline snapshot after #263 + connection enrichment"
```

## FW-2 — Fix the schema's required-section list  *(P1)*
`Skills Reference/schemas/skill-card.v1.json` still lists `Connections` and
`References` in `cardTypes.standard.requiredSections` and `cardTypes.extended.
requiredSections`, but those moved to frontmatter at migration (0 cards have them
as body sections; the validator doesn't enforce them; the schema's own
`bodySections` note already says they live in frontmatter). Trim both arrays to
the five narrative sections so the contract can't mislead future authors.

## FW-3 — Remove tracked junk and harden `.gitignore`  *(P1)*
`.DS_Store` is tracked; `_deltest` is an empty file at repo root; `_delete/`
holds a scratch `missing_skills.md`. `.gitignore` contains only `node_modules/`.
```bash
git rm --cached .DS_Store _deltest
# delete _delete/ if its content is superseded
printf 'node_modules/\n.DS_Store\n**/.DS_Store\n_deltest\n' > .gitignore
```

## FW-4 — Decide the chain step-header convention  *(P2)*
Three chains throw permanent validator warnings (see per-card tasks below).
Either rename their sections to `## Step N` or formally whitelist the alternate
patterns in `validate-cards.mjs`. Goal: a clean validator run so future warnings
get noticed.

## FW-5 — Retire or date-stamp the stale fix-report  *(P2)*
`skills-framework-fix-report.md` (2026-06-10) is superseded: its headline issue
(missing `supports` on 6 cards) is fully resolved (0 of 651 non-chain refs are
missing `supports`) and its counts predate #263. Archive or regenerate it so it
isn't mistaken for current state.

---

# Per-card update tasks

---

## #130 — Transformation Chain  `Pre-built-Chains/transformation.md`  *(P2 — content)*
The most substantive per-card finding. This chain is markedly thinner than its
siblings: its `## Steps` section is a bare 8-item list naming skill combinations,
with no per-step elaboration. Comparable chains (`org-design-for-flow`,
`improve-delivery-flow`, `mission-execution-protocol`, `work-to-be-done`) give
each step a full paragraph describing the question, the method, and the output.

- **Task:** expand each of the 8 steps to sibling-chain depth (what the step asks,
  how the named skills are applied, what artifact/decision it produces).
- **Task:** convert the list to `## Step N — …` headers (this also clears its
  `snapshot-test`/validator warning and satisfies FW-4 for this file).

---

## #117 — Capacity Planning  `Pre-built-Chains/capacity-planning.md`  *(P2)*
Uses `## The Eight Steps` instead of `## Step N` headers → standing validator
warning. **Task:** rename to `## Step N` (per FW-4) or whitelist. Content is fine.

---

## #214 — Crawl / Walk / Run  `Pre-built-Chains/crawl-walk-run.md`  *(P2)*
Uses descriptive headers (`Crawl — …`, `Walk — …`, `Run — …`) instead of
`## Step N` → standing validator warning. **Task:** decide per FW-4 — here the
Crawl/Walk/Run names are meaningful, so whitelisting may be preferable to
renaming. Content is fine.

---

## #28 — Conway's Law  `Systems/conways-law.md`  *(P2 — terminology consistency)*
The Definition calls the inverse the **"Reverse Conway Manoeuvre,"** but the
framework has a dedicated card **#155 "Inverse Conway Maneuver"** (and the
`org-design-for-flow` chain uses "Inverse Conway Maneuver").

- **Task:** standardize the term in this card to "Inverse Conway Maneuver" and
  add a connection/cross-link to #155 so the two cards reference each other.

---

## Asymmetric connections — verify each is intentional  *(P3 — low priority)*
Connections are not required to be symmetric, so these may all be deliberate
(card→chain links are naturally one-directional). Listed per source card for a
quick author confirmation; add a reciprocal connection only where it aids
navigation. The card→card pairs (top group) are the ones most worth a look; the
card→chain pairs (bottom) are expected to be one-directional.

Card→card (worth reviewing):
- **#1 Inversion** → #2 Second-Order Effects (no link back)
- **#2 Second-Order Effects** → #19 Emergence
- **#35 Efficiency** → #18 Bottlenecks, #30 Trade-offs
- **#201 Hindsight Bias** → #200 Fundamental Attribution Error
- **#203 Narrative Instinct** → #199 Survivorship Bias, #201 Hindsight Bias
- **#243 Dunbar's Number** → #118 Mission Command
- **#217 Value Creation & Capture** → #34 Monopoly vs. Competition
- **#239 Long Tail** → #84 Marketplace

Card→chain (likely intentional, no action expected):
- **#194 Leader's Intent** → #10 Pre-mortem, #127 RACI Framework
- **#212 Observer Bias** → #96 Hiring Decision (chain)
- **#219 Pricing Logic** → #94 Pricing Decision (chain)
- **#225 A/B Testing** → #214 Crawl / Walk / Run (chain)

---

# Reviewed and clean

- **All 263 cards** passed the mechanical + reference layer with no defects.
- **All 21 chains** passed the reference + step-coherence audit (only the header-
  convention items above).
- **Deep-read cards** with no content issues found: #263 Delete Before You
  Optimise; #183 AI Authority Boundaries; #184 Minimal Capability Principle;
  #185 Prompt Injection; #258 Coaching Stance; #165 Contracting & Scope (and the
  rest of Consulting-Craft); plus spot-checked #200-series Psychology, #28/#243
  Systems, Economics, Business, #146 Little's Law, #194 Leader's Intent,
  Mathematics, and Network Effects samples — all accurate, concrete, and
  well-referenced.

**Remaining work:** the line-by-line editorial pass over the cards *not* deep-read
this round. Use `review-task-list.md` as the tracker; based on this sample, expect
it to surface refinements (clarity, terminology consistency like the Conway item,
the occasional thin chain) rather than correctness errors.
